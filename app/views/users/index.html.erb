<%= render 'books/info', books: @books %>

<h1>Users</h1>

<table>
  <thead>
  <tr>
    <th>image</th>
    <th>name</th>
    <th></th>
  </tr>
  </thead>
<tbody>
  <% @books.each do |book| %>
  <tr>
    <td><%= link_to image_tag book.user.get_profole_image(100,100) %></td>
    <td><%= user.name %></td>
    <td><%= link_to "Show", book_path(book.id) %></td>
  </tr>  
  <% end %>
</tbody>
</table>
